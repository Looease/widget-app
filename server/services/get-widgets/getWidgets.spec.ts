import { describe, test } from "vitest";

describe('getWidgets', () => {
    test('should retrieve widgets from the database', () => {

    });
    test('should return a ERROR if no widgets are found', () => {

    });
})