import { describe, test } from "vitest";

describe("deleteWidgets", () => {
  test("should delete widgets", () => {});
  test("should handle loading state", () => {});
  test("should throw errors on failure", () => {});
});
